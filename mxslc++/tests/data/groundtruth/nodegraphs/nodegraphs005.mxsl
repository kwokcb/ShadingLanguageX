
mutable float r = randomfloat(seed=1);
mutable float s = randomfloat(seed=2);

[[nodegraph]]
float foo()
{
    return r + s;
}

constant(foo());

r = randomfloat(seed=3);
s = randomfloat(seed=4);

constant(foo());
